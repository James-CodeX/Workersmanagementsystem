{
    "version": 2,
    "buildCommand": "prisma generate && next build",
    "installCommand": "npm install --legacy-peer-deps",
    "functions": {
        "src/app/**/*.tsx": {
            "maxDuration": 30
        }
    },
    "regions": ["sin1"]
}